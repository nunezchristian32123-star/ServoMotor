# ServoMotor
Manipulation of servo motor using a touch screen, touch screen controls the angle of which the motor turns. 
If statement sets servo angle to zero if it reaches its upper limit angle (used for when a false touch is read)
Function call to read x and y values of touch screen for servo control value is then appended to myservo.write for manipulation.
